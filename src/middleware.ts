// This file is intentionally left empty to disable i18n middleware logic
// that was causing routing issues. An empty but valid export is required 
// to prevent Next.js from crashing. Language detection is now handled 
// directly in the root layout.

export function middleware() {
  // This function is a no-op to satisfy Next.js.
}
